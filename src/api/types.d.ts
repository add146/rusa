import { D1Database, R2Bucket } from '@cloudflare/workers-types';

declare global {
  // These are usually provided by @cloudflare/workers-types
  // but explicitly declaring them helps if tsconfig is not picking them up correctly
}
